import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    height: '100%',
    width: '100%',
    alignItems: 'center',
  },
  spotifyIcon: {
    marginTop: 150,
    marginBottom: 20,
    height: 200,
    width: 200,
  },
  connectButton: {
    marginVertical: 10,
    borderRadius: 10,
    backgroundColor: '#A753AA',
  },
});
